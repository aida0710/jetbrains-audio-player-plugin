# SP5: A-Bループ Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** 波形上で A/B 点を指定し、その区間だけ繰り返し再生する。

**Architecture:** ループ判定は純粋関数 `shouldLoopBack` でTDD、実行は100msタイマーのポーリング（速度に非依存）。マーカー/シェードは TimelineImagePanel が描画。

**設計仕様:** `docs/superpowers/specs/2026-06-16-sp5-ab-loop-design.md`

**共通の注意:** コミット前 `./gradlew ktlintFormat`。`runIde` 実行しない。`build-signed.sh`/`build.gradle.kts` に触れない。各タスク列挙ファイルのみ `git add`。各タスク後 `./gradlew test`+`buildPlugin` 緑。

---

## Task 1: shouldLoopBack（純粋）＋ TimelineImagePanel マーカー

**Files:** Modify `AudioPlayerService.kt`, `TimelineImagePanel.kt`; Test `AudioPlayerServiceTest.kt`.

- [ ] **Step 1: Failing test** — add to `AudioPlayerServiceTest.kt`:

```kotlin
    @Test
    fun `shouldLoopBack true when current reaches B`() {
        assertTrue(AudioPlayerService.shouldLoopBack(4_000_000, 1_000_000, 4_000_000))
        assertTrue(AudioPlayerService.shouldLoopBack(5_000_000, 1_000_000, 4_000_000))
    }

    @Test
    fun `shouldLoopBack false before B or invalid range`() {
        assertFalse(AudioPlayerService.shouldLoopBack(3_000_000, 1_000_000, 4_000_000))
        assertFalse(AudioPlayerService.shouldLoopBack(5_000_000, -1, 4_000_000))
        assertFalse(AudioPlayerService.shouldLoopBack(5_000_000, 4_000_000, 4_000_000))
    }
```

- [ ] **Step 2: Verify fail** — `./gradlew test --tests "com.github.audioplayer.AudioPlayerServiceTest"` → compile error.

- [ ] **Step 3: Implement** — add to `AudioPlayerService.kt` companion:

```kotlin
        fun shouldLoopBack(
            current: Long,
            a: Long,
            b: Long,
        ): Boolean = a >= 0 && b > a && current >= b
```

- [ ] **Step 4: Verify pass** — `./gradlew test --tests "com.github.audioplayer.AudioPlayerServiceTest"` → PASS.

- [ ] **Step 5: TimelineImagePanel markers** — add two properties (near `positionMicros`):

```kotlin
    var aMarkerMicros: Long = -1
        set(value) { field = value; repaint() }

    var bMarkerMicros: Long = -1
        set(value) { field = value; repaint() }
```

Add marker colors to the companion (near PLAYHEAD_COLOR):

```kotlin
        private val AB_MARKER_COLOR = JBColor(Color(0x00, 0x96, 0x88), Color(0x26, 0xA6, 0x9A))
        private val AB_REGION_COLOR = Color(0x00, 0x96, 0x88, 0x33)
```

In `paintComponent`, inside the `if (ve <= vs || width <= 0) return` guard block (i.e. after the `return` guard, where `vs`/`ve` are valid), add marker/region drawing AFTER the ruler loop and BEFORE the playhead block:

```kotlin
        val a = aMarkerMicros
        val b = bMarkerMicros
        if (a in vs..ve || b in vs..ve || (a in 0..vs && b >= ve)) {
            if (a in 0 until b) {
                val ax = xAtTime(a.coerceIn(vs, ve), width, vs, ve).coerceIn(0, width)
                val bx = xAtTime(b.coerceIn(vs, ve), width, vs, ve).coerceIn(0, width)
                g.color = AB_REGION_COLOR
                g.fillRect(ax, 0, (bx - ax).coerceAtLeast(0), height)
            }
        }
        if (a in vs..ve) {
            g.color = AB_MARKER_COLOR
            g.fillRect(xAtTime(a, width, vs, ve).coerceIn(0, width - 1), 0, 2, height)
        }
        if (b in vs..ve) {
            g.color = AB_MARKER_COLOR
            g.fillRect(xAtTime(b, width, vs, ve).coerceIn(0, width - 1), 0, 2, height)
        }
```

- [ ] **Step 6: Verify build** — `./gradlew test` (full), `./gradlew buildPlugin`.

- [ ] **Step 7: Format and commit**

```bash
./gradlew ktlintFormat
git add src/main/kotlin/com/github/audioplayer/AudioPlayerService.kt \
        src/main/kotlin/com/github/audioplayer/TimelineImagePanel.kt \
        src/test/kotlin/com/github/audioplayer/AudioPlayerServiceTest.kt
git commit -m "feat: A-Bループ判定(shouldLoopBack)と波形のA/Bマーカー描画を追加"
```

---

## Task 2: AudioPlayerPanel の A-B UI＋ループ実行

**Files:** Modify `AudioPlayerPanel.kt`. `runIde` 実行不可 → `buildPlugin`+`test`。

Read the current `AudioPlayerPanel.kt` first.

- [ ] **Step 1: Fields** — add:

```kotlin
    private var loopAMicros = -1L
    private var loopBMicros = -1L
    private var abLoopEnabled = false
    private val setAButton = JButton("A")
    private val setBButton = JButton("B")
    private val abLoopToggle = JToggleButton("A-B")
    private val abClearButton = JButton("ABクリア")
```

- [ ] **Step 2: Place in analyze panel** — in `createAnalyzePanel()`, add to the `buttonPanel` (after the zoom/split controls): `add(setAButton); add(setBButton); add(abLoopToggle); add(abClearButton)`. Tooltips: setA="区間の開始(A)を現在位置に", setB="区間の終了(B)を現在位置に", abLoopToggle="A-B区間をリピート", abClear="A-Bをクリア".

- [ ] **Step 3: Helpers** — add:

```kotlin
    private fun applyMarkers() {
        timelinePanel.aMarkerMicros = loopAMicros
        timelinePanel.bMarkerMicros = loopBMicros
    }
```

- [ ] **Step 4: Listeners** — in `setupListeners()`:

```kotlin
        setAButton.addActionListener {
            loopAMicros = playerService.currentMicroseconds
            if (loopBMicros in 0..loopAMicros) loopBMicros = -1
            applyMarkers()
        }
        setBButton.addActionListener {
            val pos = playerService.currentMicroseconds
            if (loopAMicros in 0 until pos) {
                loopBMicros = pos
                applyMarkers()
            }
        }
        abLoopToggle.addActionListener {
            abLoopEnabled = abLoopToggle.isSelected && loopAMicros >= 0 && loopBMicros > loopAMicros
            abLoopToggle.isSelected = abLoopEnabled
        }
        abClearButton.addActionListener {
            loopAMicros = -1
            loopBMicros = -1
            abLoopEnabled = false
            abLoopToggle.isSelected = false
            applyMarkers()
        }
```

- [ ] **Step 5: Loop execution in timer** — in `startPositionTimer()`'s `Timer(100) { ... }` block, after `current`/`total` are obtained (near where position is updated), add:

```kotlin
                    if (abLoopEnabled && AudioPlayerService.shouldLoopBack(current, loopAMicros, loopBMicros)) {
                        playerService.seek(loopAMicros)
                    }
```

(Ensure `current` here is `playerService.currentMicroseconds` — the original-timeline value already used by the timer.)

- [ ] **Step 6: Verify** — `./gradlew buildPlugin` (success), `./gradlew test` (full green).

- [ ] **Step 7: Format and commit**

```bash
./gradlew ktlintFormat
git add src/main/kotlin/com/github/audioplayer/AudioPlayerPanel.kt
git commit -m "feat: A-B区間の指定/クリア/リピート再生のUIを追加"
```

---

## 完了条件
- `./gradlew test` 緑、`./gradlew buildPlugin` 成功、`./gradlew ktlintCheck` 違反なし。
- `shouldLoopBack` テスト済み。A-Bループは100msタイマーのポーリングで速度非依存。
